# Runtime dependencies for the API service.
#
# Deliberately excludes torch / transformers / peft / ultralytics: they add
# ~800 MB installed and the 1.5B model needs ~3 GB RAM to load, which does not
# fit a small instance. Every one of those imports is lazy, so the service runs
# without them and answers from the tool-grounded path.
#
# For the local LLM and CCTV detection, install requirements-ml.txt as well and
# set ENABLE_LOCAL_LLM=true on an instance with >= 4 GB RAM.

fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
python-multipart>=0.0.9
python-dotenv>=1.0

httpx>=0.27

# Geometry: used by core/geo.py for spatial queries.
shapely>=2.0
pyproj>=3.6

# core/vision.py imports these at module load.
numpy>=1.26
Pillow>=10.0
